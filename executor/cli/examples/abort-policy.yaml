metadata:
  name: "test-abort-policy"
spec:
  rules:
    - match:
        method:
          exact: "GET"
        path:
          exact: "/"
      fault:
        percentage: 100
        abort:
          httpStatus: 503
