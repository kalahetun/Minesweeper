# Delay 故障注入策略
# 功能：延迟请求响应指定时间
# 场景：测试服务对慢响应的超时处理

metadata:
  name: "delay-policy"
spec:
  rules:
    - match:
        method:
          exact: "GET"
        path:
          exact: "/"
      fault:
        percentage: 100          # 100% 触发
        delay:
          fixed_delay: "1000ms"  # 延迟 1 秒
