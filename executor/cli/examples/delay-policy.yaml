metadata:
  name: "test-delay-policy"
spec:
  rules:
    - match:
        method:
          exact: "GET"
        path:
          exact: "/test"
      fault:
        percentage: 100
        delay:
          fixed_delay: "1000ms"
