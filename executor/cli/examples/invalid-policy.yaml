# 无效策略示例（用于测试错误处理）
# 缺少 name 字段，应该被拒绝

metadata:
  # name: missing - this should cause validation error
spec:
  rules:
    - match:
        path:
          exact: "/api/test"
      fault:
        percentage: 50
        abort:
          httpStatus: 500
